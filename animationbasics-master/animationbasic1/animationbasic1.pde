void setup() {    // happens at the beginn ing of a sketch and then never again!
  size(800,600);
}

void draw()  {    // happens throughout the sketch and is called 60 times/second
  ellipse(400,300,200,200);
}

                  // One you have included setup and draw, your sketch is now in
                  // active mode.
